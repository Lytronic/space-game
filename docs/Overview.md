# Overview

Since this is a Godot game, all elements of it are arranged in a node graph. The C# scripts are then attached to their respective nodes and can access other nodes using the `GetNode<>()` method.
